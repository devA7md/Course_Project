image: node:latest

stages:
- build
- test

variables:
  DB_URI: 'mongodb://ecommerce:qwerty15@ds026558.mlab.com:26558/project'
  PORT: 1122
  SECRET_TOKEN: 'aldjfdhfkasdgkajgljfdghakdsjgasdhfaksdg'

cache:
  paths:
  - node_modules/

install dependencies:
  stage: build
  script:
  - npm install
  artifacts:
    paths:
    - node_modules/

run my test:
  stage: test
  services:
  - mongo:4
  script:
  - npm run test:ci
  - exit 0
